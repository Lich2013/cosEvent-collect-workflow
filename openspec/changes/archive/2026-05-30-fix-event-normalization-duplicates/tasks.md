## 1. 智能城市解析器重构与城市收拢

- [x] 1.1 在 `src/utils/parsers.py` 中重写 `parse_city` 函数，加入省份前缀（如“浙江省”）剥离逻辑。
- [x] 1.2 在 `parse_city` 中引入高频会展大市字典强匹配，支持包含“哈尔滨”、“石家庄”等特例。
- [x] 1.3 在 `parse_city` 中加入行政后缀剥离（“市/区/县”），并为无法识别的名称引入前两字截取兜底，确保不再产生四字伪城市。

## 2. 入口 O(1) 字典匹配与时间窗口秒配

- [x] 2.1 修改 `src/services/fusion_service.py` 中的 `EventFusionService.find_or_create_normalized_event`，在最头部加入标准名与别名 O(1) 字典快速配对通道。
- [x] 2.2 在 O(1) 配对通道中添加 7 天档期时间窗口强校验，防止同城同名不同届活动被错误合并。

## 3. 动态时空纠偏与级联融合

- [x] 3.1 在 `find_or_create_normalized_event` 快速通道中，加入跨“未知”城市的级联纠偏与升级合并逻辑。
- [x] 3.2 如果在“未知”城市下找到了完全同名的超级节点，但目标具体城市的对应节点尚不存在，则就地 UPDATE “未知”节点的城市和 fingerprint 以完成就地物理升级。
- [x] 3.3 如果具体城市节点已存在，则将日程级联重定向到具体城市节点，并安全级联清理无用“未知”节点。

## 4. 测试与验证

- [x] 4.1 编写单元测试，覆盖省份清洗、核心会展大市匹配、二字兜底等城市提取边界。
- [x] 4.2 编写集成测试，验证跨未知城市的时空纠偏、同城同名不同届的时间窗口拦截等场景，运行全量测试套件保障 100% 回归成功。
