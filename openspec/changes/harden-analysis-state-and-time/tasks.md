## 1. 基础工具与异常分类

- [x] 1.1 新增统一北京时间工具模块，提供当前时间、当前日期字符串和测试可 monkeypatch 的入口。
- [x] 1.2 将模板渲染、查询过滤、物化冷冻窗口、活动持久化中触达的当前日期读取切换到统一时钟。
- [x] 1.3 新增或定义 LLM 暂时性异常类型，并梳理 Agent pipeline 中的异常抛出语义。

## 2. Agent Prompt 与分析状态机加固

- [x] 2.1 新增 Triage Agent system prompt 模板，并在创建 Triage Agent 时使用模板渲染结果。
- [x] 2.2 调整候选人核验 Prompt，使核心规则作为 Agent `instructions` 渲染，候选人姓名和博文列表作为输入上下文传递。
- [x] 2.3 修改共识分析中“所有 extractor 失败”的处理，使其作为暂时性失败保留 `raw_posts.is_analyzed = 0`。
- [x] 2.4 确认 Pydantic 校验、枚举断言和数据库完整性冲突仍会触发 `raw_posts.is_analyzed = 2`。

## 3. 活动持久化与历史日程兜底

- [x] 3.1 在保存提取结果前增加历史活动硬过滤，标准日期早于统一当前日期时跳过并记录审计日志。
- [x] 3.2 确保被跳过的历史活动不会创建或更新 `normalized_events`、`event_aliases`、`event_mappings`。
- [x] 3.3 保持 `未知` 日期和合法未来日期沿用现有置信度过滤、类型校验和融合流程。

## 4. 候选人状态流转加固

- [x] 4.1 调整候选人抓取/核验逻辑，抓取不可确认或 scraper 超时降级时保留 pending，不直接 ignored。
- [x] 4.2 调整无博文场景的状态判断，只有确认无内容且无二次元证据时才允许高置信忽略，否则保留 pending 或进入 undetermined。
- [x] 4.3 修改候选人重复发现合并逻辑，保留既有 `is_verified = 1` 与 `verify_reason`，同时非破坏性合并新增 UID。

## 5. 测试与验证

- [x] 5.1 修复导出范围测试，使用统一时钟冻结参考日期，消除真实日期依赖。
- [x] 5.2 新增测试覆盖所有 extractor 临时失败不触发 `is_analyzed = 2`。
- [x] 5.3 新增测试覆盖历史活动不会以 `未开始` 状态写入，也不会触发归一化节点创建。
- [x] 5.4 新增测试覆盖候选人不可确认抓取结果保留 pending，以及重复发现不覆盖已核验结果。
- [x] 5.5 运行 `uv run pytest` 并确认完整测试通过。
